in caso di Address already in use
Port 6001 is in use by another program. Either identify and stop that program, or start the server with a different port.

comandi:
sudo netstat -nlp | grep <numero porta>
kill <PID>


ps aux | grep simple_db -> prende tutti i processi associati a simple_db

history | grep POST -> prende tutti i comandi in cui era presente la parola POST
